# توثيق مشروع "دفتر" (Daftar Documentation)

هذا المستند يعتبر المرجع الشامل لمشروع "دفتر" للمطورين الحاليين والمستقبليين. يوضح هذا المستند البنية التحتية، التقنيات المستخدمة، هيكل قاعدة البيانات، وقواعد العمل (Business Logic) الخاصة بالتطبيق.

---

## 1. نظرة عامة على المشروع (Project Overview)
"دفتر" هو تطبيق ويب مصمم كـ PWA (تطبيق ويب تقدمي) يعمل بكفاءة على المتصفحات وأجهزة الموبايل. يهدف التطبيق إلى إدارة الديون والمشتريات بين المستخدم والمحلات التجارية، بالإضافة إلى تتبع المدفوعات الجزئية والكلية وإرفاق إيصالات وصور للمنتجات.

التطبيق يدعم اللغة العربية بشكل أساسي (RTL) ويعتمد على واجهة مستخدم حديثة وبسيطة.

---

## 2. التقنيات المستخدمة (Tech Stack)

### الواجهة الأمامية (Frontend)
- **الإطار العام:** Next.js (App Router)
- **المنطق والتفاعل:** React (Client Components)
- **لغة البرمجة:** TypeScript
- **الستايل:** CSS عادي (Vanilla CSS) باستخدام متغيرات CSS لبناء تصميم متجاوب ويدعم الوضع الداكن/الفاتح (متوفر كمتغيرات).
- **التوافق مع الموبايل:** تم تحسين واجهات الإدخال ورفع الصور لدعم الكاميرا مباشرة (PWA Ready).

### الواجهة الخلفية (Backend)
- **الإطار العام:** NestJS
- **لغة البرمجة:** TypeScript
- **ORM:** Prisma (v7.10.0) مع `PrismaPg` Driver Adapter.
- **قاعدة البيانات:** PostgreSQL
- **رفع الصور:** Cloudinary API

---

## 3. هيكل قاعدة البيانات (Database Schema)

قاعدة البيانات تتكون من 4 جداول رئيسية:

1. **Shop (المحلات):**
   - `id`: المعرف.
   - `name`: اسم المحل.
   - `phone` / `notes`: معلومات إضافية.
2. **Buyer (المشترين / المسددين):**
   - `id`, `name`.
3. **Purchase (المشتريات / الفواتير):**
   - يربط بين المحل (`shopId`) والمشتري (`buyerId`).
   - يحتوي على `totalAmount` (الإجمالي) و `paidAmount` (المدفوع).
   - `paymentStatus`: (حالة الدفع: `UNPAID`, `PARTIALLY_PAID`, `PAID`).
   - `paidById` و `paidAt`: لتتبع من سدد الفاتورة ومتى.
4. **PurchaseImage (صور الفاتورة):**
   - `imageUrl`: رابط الصورة المرفوعة على Cloudinary.
   - `isReceipt`: (Boolean) لفصل صور المنتجات (false) عن صور إيصالات السداد (true).

---

## 4. قواعد العمل والخوارزميات (Business Logic)

- **العمليات الحسابية:** 
  جميع الحسابات الهامة تتم في Backend داخل `PurchasesService` لضمان أمان البيانات.
  - `remainingAmount` (المتبقي) = `totalAmount` - `paidAmount`. يتم حسابه وإرجاعه في كل طلب (Request).
  - لا يمكن أن يتجاوز المبلغ المدفوع المبلغ الإجمالي (يُرجع Backend خطأ 400).
  
- **تحديث حالة الدفع (Payment Status):**
  يتم تحديثها تلقائيًا في Backend:
  - إذا كان المدفوع 0 ← `UNPAID`
  - إذا كان المدفوع أكبر من 0 وأقل من الإجمالي ← `PARTIALLY_PAID`
  - إذا كان المدفوع يساوي أو أكبر من الإجمالي ← `PAID`

- **إدارة الصور:**
  - يتم استخدام Cloudinary لتخزين الصور.
  - يتم تمييز الصورة ما إذا كانت للمنتج أو لإيصال السداد باستخدام متغير `isReceipt`.
  - في الموبايل، تم تفعيل خاصية `capture="environment"` لفتح الكاميرا الخلفية فوراً لالتقاط إيصالات أو منتجات.

---

## 5. هيكل الملفات والمجلدات (Folder Structure)

```text
daftar/
├── backend/
│   ├── prisma/             # مخطط قاعدة البيانات (schema.prisma)
│   ├── src/
│   │   ├── prisma/         # إعدادات اتصال Prisma Adapter
│   │   ├── cloudinary/     # خدمة الرفع على Cloudinary
│   │   ├── dashboard/      # إحصائيات الصفحة الرئيسية
│   │   ├── shops/          # عمليات المحلات (CRUD)
│   │   ├── buyers/         # عمليات المشترين (CRUD)
│   │   └── purchases/      # عمليات المشتريات، الدفع، والصور
│   └── .env                # متغيرات البيئة (DB & Cloudinary)
│
└── frontend/
    ├── app/
    │   ├── layout.tsx      # الهيكل الأساسي (Navbar)
    │   ├── page.tsx        # الداشبورد الرئيسية
    │   ├── shops/          # واجهات المحلات والتفاصيل
    │   ├── buyers/         # واجهات المشترين
    │   └── purchases/      # إضافة وتعديل الفواتير
    ├── components/
    │   ├── PaymentModal.tsx # نافذة السداد والدفع
    │   ├── PurchaseForm.tsx # نموذج إضافة/تعديل فاتورة مخصص للموبايل
    │   └── ImageGallery.tsx # عارض الصور
    └── lib/
        ├── api.ts          # دوال التخاطب مع الـ Backend
        └── utils.ts        # دوال مساعدة (تنسيق التواريخ، الخ)
```

---

## 6. خطوات تشغيل المشروع (Running the Project)

1. **البيئة الخلفية (Backend):**
   ```bash
   cd backend
   npm install
   npx prisma generate
   npm run start:dev
   ```
   *(تأكد من إعداد `.env` وإضافة قاعدة البيانات ومفاتيح Cloudinary)*

2. **البيئة الأمامية (Frontend):**
   ```bash
   cd frontend
   npm install
   npm run dev
   ```
   
---
*تم إعداد هذا المستند ليكون دليلاً حياً يتم الرجوع إليه عند إضافة ميزات جديدة أو عند تسليم المشروع لمطور آخر.*
