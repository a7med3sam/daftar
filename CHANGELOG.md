# سجل التغييرات والمهام (Changelog & Task Logs)

هذا الملف مخصص لتتبع جميع المهام التي تم إنجازها في المشروع، ليكون مرجعاً واضحاً لأي مطور يعمل على النظام لمعرفة مسار التطور، والمشاكل التي حُلت، والميزات التي تم إضافتها.

---

## [الإصدار 1.0.0] - أغسطس 2026 - مرحلة بناء الأساس (MVP Phase)

### 🚀 الميزات الجديدة (Features)
- **إنشاء هيكل المشروع الأساسي:** تم فصل المشروع إلى Backend (NestJS) و Frontend (Next.js).
- **قاعدة البيانات (Prisma & Postgres):** إعداد `schema.prisma` لتدعم جداول المحلات، المشتريين، والمشتريات وصور المشتريات.
- **إدارة المحلات والمشترين:** واجهات لإضافة وعرض المحلات والمشترين.
- **نظام المشتريات:** واجهة مخصصة لإضافة فاتورة/شراء جديد وربطه بالمحل والمشتري.
- **الداشبورد (Dashboard):** عرض إحصائيات عامة (الإجمالي، المتبقي، المدفوع، وأحدث العمليات).

### 💳 نظام السداد (Payment System)
- إضافة أيقونة "سداد" (💵) لجدول المشتريات.
- بناء نافذة `PaymentModal` منبثقة تسمح بسداد جزء من المبلغ أو المبلغ بالكامل.
- **المنطق البرمجي (Backend Logic):** 
  - حساب المبلغ المتبقي (`remainingAmount`) تلقائياً.
  - منع تجاوز المبلغ المدفوع للمبلغ الإجمالي.
  - تحديث حالة الدفع تلقائياً (`UNPAID`, `PARTIALLY_PAID`, `PAID`).
  - تسجيل بيانات الشخص الذي قام بالسداد وتاريخ السداد.

### 🖼️ نظام الصور (Image System & PWA Optimization)
- إعداد خدمة رفع الصور باستخدام **Cloudinary**.
- فصل صور المنتجات عن صور إيصالات السداد في قاعدة البيانات عبر حقل `isReceipt` (Boolean).
- عرض صور المنتجات وصور الإيصالات بشكل منفصل في جدول المشتريات.
- بناء مكون `ImageGallery` لعرض الصور بنظام التمرير (Swipe).
- **تحسينات للموبايل (PWA):**
  - نقل قسم رفع الصور للأعلى في نموذج الشراء الجديد.
  - إضافة زر مخصص (📷 فتح الكاميرا) يستخدم `capture="environment"` لفتح كاميرا الموبايل فوراً.
  - إضافة زر (🖼️ اختر من المعرض) للصور العادية.
  - إظهار معاينة مصغرة للصور مع إمكانية حذف الصورة قبل رفعها.

### 🐛 إصلاح الأخطاء (Bug Fixes)
- إصلاح خطأ `PrismaClientInitializationError` في NestJS عن طريق إعداد `PrismaPg` Adapter وتوفير رابط الاتصال من متغيرات البيئة بالشكل الصحيح باستخدام `dotenv/config`.
- إصلاح خطأ Type Error متعلق بحقل `isReceipt` بإجباره على نوع `Boolean()` الصريح لإرضاء `Prisma 7.10.0` الذي يعامله كـ `boolean | null`.
- إصلاح خطأ انهيار الـ Frontend عند تمرير فواتير بدون حقل `remainingAmount` عن طريق حسابه محلياً كإجراء أمان احتياطي.
- تصحيح اسم `CLOUDINARY_CLOUD_NAME` في الـ `.env` الخاص بالخادم.

---

## 📌 مهام مستقبلية مقترحة (Future Todo)
*أضف هنا أي مهام أو أفكار تريد تنفيذها في المستقبل:*

- [ ] تحويل التطبيق رسمياً إلى PWA عن طريق إضافة `manifest.json` و `service-worker`.
- [ ] إضافة نظام تسجيل الدخول والمصادقة (Authentication).
- [ ] تحسين نظام الفلترة والبحث للمحلات والمشتريات.
- [ ] تصدير الفواتير كـ PDF أو Excel.
- [ ] ...
